# 审核规则

## 四种结果

- `通过待人工确认`：来源、范围、证据和任务字段完整，仍需用户确认。
- `待补证据`：策略方向可保留，但缺少列明的执行前证据。
- `退回策略Agent`：来源命中不存在、品牌门店范围不一致、事实与动作不匹配、验证指标缺失或结构错误。
- `安全拒绝`：包含刷单、虚假交易、诱导评价、电话追评、未授权顾客触达或自动平台操作。

## 状态限制

审核后的任务只能处于 `待补数据`、`待补证据` 或 `待人工确认`。不得将候选策略写成执行结果。动作库外的新策略在独立审核通过前不得进入任务列表。
